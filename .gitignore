# Build artifacts
target/
build/
build-*/

# Environment (generated by `make infra`)
.env
.env.taker

# Scaffold: wallet, circuits (Makefile), LEZ cache (see scaffold.toml) — all under .scaffold/
.scaffold/

# Claude Code
.claude
CLAUDE.md

# Working docs
plan.md
workstreams/
insights.md
cold-clone-checklist.md
demo-script.md

# Logos module-builder Nix outputs (per-flake)
swap-module/result
swap-module/result-*
swap-module/.direnv
swap-module/generated_code/
swap-ui/result
swap-ui/result-*

# Two-Basecamp dogfooding instances (created by `make basecamp-init-{maker,taker}`)
.basecamp/
swap-ui/.direnv
swap-ui/flake.lock

# Logos dev-boost / agent context generated locally; not part of source
.agents/
.opencode/
.mcp.json
.sisyphus/
AGENTS.md

# Editor-generated agent rules
.cursor/rules/logos.mdc

# Nix result symlinks
result
result-*
result-basecamp
